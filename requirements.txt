# All Week 09-12 projects use only the Python standard library.
# The packages below are only for the learning environment / testing.

# ── Jupyter Environment ───────────────────────
jupyter>=1.0.0
notebook>=7.0.0
ipykernel>=6.25.0

# ── Testing (introduced in Week 08, used by projects) ───
pytest>=8.0.0
